#!/bin/sh
set -eu
kit_root=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
command="$kit_root/.venv/bin/echelle"
if [ ! -x "$command" ]; then
  printf '%s\n' "The NIFS kit is not installed. Run ./install.sh offline (or online) first." >&2
  exit 2
fi
exec "$command" "$@"
